# Print stats of raw_text and summary_text
def print_summary_stat(raw_text, summary_text):
    # length(raw_text)
    # length(summary_text)
    # weight(raw_text)
    # weight(summary_text)
    # weight_density(raw_text)
    # weight_density(summary_text)
    # weight_inc(summary_text over raw_text)
    # len_dec(summary_text over raw_text)
    print("length of original text = " + str(len(raw_text)))
    print("length of summary text = " + str(len(summary_text)))
    